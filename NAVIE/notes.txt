Pointers:

Ensure string is being appended to knowledge.csv or change the usage in the "model_name_rate_min"

Wtf is going on with model dict when you need to pass the img through it: how to not hardcode and adapt it

Possible edits:
    Very low priority:
        Change the user input for Modify NAIVE to showing default setting but editable instead of adding all models again

Documentation updates:
    accepted img formats: jpeg, jpg, png
    spellings